# DJANGO CONFIGURATIONS
DEBUG=True
SECRET_KEY=["secret-key-django"]
SERVER=0.0.0.0
DJANGO_SETTINGS_MODULE=core.settings
ALLOWED_HOSTS=localhost,127.0.0.1,[ip-do-host]
CSRF_TRUSTED_ORIGINS=http://localhost,http://[ip-do-host]
CORS_ORIGIN_ALLOW_ALL=True
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:4200

# POSTGRES CONFIGURATIONS
POSTGRES_USER=[usuario-banco]
POSTGRES_PASS=[senha-usuario-banco]
POSTGRES_PORT=5432
POSTGRES_DB_NAME=[nome-do-banco]
POSTGRES_HOST=[host-do-banco]
